services:
  - type: web
    name: wcl-compare
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_app:app
    envVars:
      - key: WCL_API_KEY
        sync: false
      - key: PYTHONUNBUFFERED
        value: "true"
